电话面试-大概20分钟
1.自我介绍一分钟
2.Hive的存储格式有哪些：
    TextFile：TextFile文件不支持块压缩，默认格式，数据不做压缩，磁盘开销大，数据解析开销大。
    SequenceFile：压缩数据文件可以节省磁盘空间，但是压缩效率低下，存储方式：行存储。
    OCFile: 数据按行分块，每块按列存储。结合了行存储和列存储的优点
    ORCFile:数据按行分块，每块按照列存储。压缩快，快速列存取。效率比rcfile高，是rcfile的改良版本。
3.基于Hadoop（HDFS）的数据库？
    Hbase,hive(严格来讲不是数据库，广义上来讲是一种数据仓库，实际上只是文件的一种视图)
4.Hive和HBase的区别：
    共同点：Hive和HBase都是架构在Hadoop上的，都是基于HDFS作为底层存储
    不同点：
          1.Hive本质上不是数据库，通过元数据描述HDFS上的结构化文本数据，Hive实际上是一个ql引擎，负责将SQL翻译为MapReduce来处理数据
          2.HBase是一个列式数据库，可以用来存储非结构化数据的K-V表。
          3.HBase负责组织文件的存储方式，用来存储索引，方便查询
          4.HBase是物理表，Hive只是一个逻辑表
          5.Hive是用于批处理，实时性不高。HBase用于事务处理，实时性高
          6.Hive是一个ql引擎，HBase是一个存储引擎
          7.Hive可以进行多表关联查询，HBase不适合多表关联查询。
5.三次握手过程？
    这个很简单，可以按自己的理解组织语言说一下，要注意几点：
    1.三次握手的过程中客户端和服务端的连接状态变化情况
    2.说清楚每次是什么数据包？ACK包还是SYN包还是ACK-SYN包？
    3.说清楚每次握手的过程中ack和syn
    4.为什么要三次握手？两次可以吗？三次可以吗？

6.看我简历上写的掌握的语言和框架特别多，项目也很多平时是怎么学习的？
    balabala

7.实习过程中遇到的困难？以及怎么解决的？
    balabala

8.flink精确一次（Exactly-once）语义怎么实现的？
    通过两阶段提交和状态保存实现端对端的一致性语义
    1.开始事务（beginTransaction）创建一个临时文件夹，来写把数据写入到这个文件夹里面
    2.预提交（preCommit）将内存中缓存的数据写入文件并关闭
    3.正式提交（commit）将之前写完的临时文件放入目标目录下。这代表着最终的数据会有一些延迟
    4.丢弃（abort）丢弃临时文件
    5.若失败发生在预提交成功后，正式提交前。可以根据状态来提交预提交的数据，也可删除预提交的数据

9.讲讲flink反压（背压）？
    flink内部是基于生产者-消费者模型来进行消息传递，反压也是基于这个模型。Flink内部使用了分布式阻塞队列，当下游消费者变慢，上游会受到阻塞

10.如果在Hive增加一个字段？
    分区表增加字段会出现以前的分区中的新字段都是null,需要加一个cascade关键字

11.流量控制和拥塞控制？
    1. 流量控制：滑动窗口机制
       在数据通信中，流量控制是管理两个节点之间数据传输速率的过程，以防止快速发送方压倒慢速接收方。
       它为接收机提供了一种控制传输速度的机制，这样接收节点就不会被来自发送节点的数据淹没。
       可见流量控制是端到端微观层面的数据策略，双方在数据通信的过程中并不关心链路带宽情况，只关心通信双方的接收发送缓冲区的空间大小，可以说是个速率流量匹配策略。
    2.拥塞控制：
        在某段时间，若对网络中某一资源的需求超过了该资源所能提供的可用部分，网络的性能就要变坏。这种情况就叫拥塞。
        拥塞控制就是为了防止过多的数据注入到网络中，这样就可以使网络中的路由器或链路不致过载。
        ！！网络拥塞的本质就是 对网络资源的需求大于可用资源 ，可用资源是有限的，因此网络拥塞问题是一直存在的
        为了进行拥塞控制，TCP发送方要维持一个 拥塞窗口(cwnd) 的状态变量。
        拥塞控制窗口的大小取决于网络的拥塞程度，并且动态变化。发送方让自己的发送窗口取为拥塞窗口和接收方的接受窗口中较小的一个。
        通过启动、拥塞避免、快速重传、快速恢复

最后面试官说请注意查收短信和邮件准备下次面试

面试体验：
    1.10点还迷糊着呢就准时给我打电话，跳下床迷迷糊糊的说了自我介绍
    2.不知道为啥没问Java的知识，我Java还是很OK的。。
    3.一直在扯皮Hive和HBase,我项目中并没有用过HBase,给我的感觉是他们还是就是传说中的sql-boy？？？
    4.通过面试来看感觉招银技术一般