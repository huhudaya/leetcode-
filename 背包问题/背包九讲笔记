1.01背包为什么要逆序？
    因为dp的递推公式是 dp[i][j] = max(dp[i-1][j], dp[i-1][j-nums[i]])
    此时dp[i][j]只和上一行的值有关系，但是要压缩为dp[j]的时候，就需要和同一行的值进行比较
    为了使得压缩之后和之前有一样的效果，所以采用逆序遍历的方式，使从大到小往前遍历，这样同一行前面的值不会影响到后面的值！
    这样可以保证只算了一次
2.当恰好装满求最值的时候，为什么除了0需要设置为0，其他位置都要初始化为-inf呢？
    因为恰好装满的时候，需要只和0位置有关系！所以其他位置都需要设置为最大或者最小值！！表示无法达到！